import 'package:flutter/material.dart';

class ArchiveTab extends StatelessWidget {
  const ArchiveTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('アーカイブ / 既読一覧（実装予定）'),
    );
  }
}
